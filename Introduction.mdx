# Introduction to DocumentationAI

This is the best sub text ever that you can have in this world. I am writing some shit which i might forget in near future. as

<Callout uid="0012f037-326e-4aa6-9137-ec9950d4ace7" kind="note" collapsed="false">
  This is the note of the year: dont note anything.
</Callout>

## This is another sub heading of the year

this will gave code blocks so get ready for some code.

<CodeGroup>
  ```javascript
  // Add your code here
  const baseExtensions = [
        EditorState.readOnly.of(true),
        ...(languageExtension ? [languageExtension] : []),
        lineNumbers(),
        foldGutter(),
        EditorView.lineWrapping,
        // Add syntax highlighting
        syntaxHighlighting(defaultHighlightStyle, { fallback: true }),
        // Add theme configuration for better visual styling
        EditorView.theme({
          '&': {
            height: '100%',
            fontFamily: `"JetBrains Mono", ui-monospace, SFMono-Regular, Menlo, Monaco, Consolas, "Liberation Mono", "Courier New", monospace`,
          },
          '.cm-scroller': {
            fontFamily: 'inherit',
          },
          // Ensure proper contrast for syntax highlighting
          '.cm-content': {
            caretColor: 'var(--body)',
          },
        }),
      ];
  ```

  ```python
  def news {
    print("new")
  }
  ```
</CodeGroup>

Now code group is done, lets add an individual code block here

```javascript
 if (mergeViewRef.current) {
    mergeViewRef.current.destroy();
 }
```

<Callout uid="b65f6273-fbc5-496a-a1ec-d7a9d3df4722" kind="warning" collapsed="false">
  take care of the code this is my humble request &#x20;
</Callout>

## This one more heading of the year

this will have image a beautiful mountain view that you have never seen before

<Image uid="d7bda81d-2d5b-43e1-89ef-7ad11df726c2" src="https://images.unsplash.com/photo-1506905925346-21bda4d32df4?w=800&h=600&fit=crop" width="800" height="600" alt="Mountain landscape" />

 
